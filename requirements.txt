fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
twilio>=9.0,<10
anthropic>=0.34,<2.0
openai>=1.40,<2.0
elevenlabs>=1.50,<3.0
python-dotenv>=1.0,<2.0
websockets>=12.0,<18.0
sqlalchemy>=2.0,<3.0
phonenumbers>=8.13,<9.0
pytz>=2024.1

# Multilingual TTS (Urdu/Hindi) - free Microsoft neural voices, no API key
edge-tts>=7.0
miniaudio>=1.61

# Encryption at rest (SQLCipher). On Windows, sqlcipher3-binary wheels are
# unavailable - the app falls back to plain sqlite3 with a loud warning.
sqlcipher3-binary>=0.5; sys_platform != "win32"

# Testing
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
